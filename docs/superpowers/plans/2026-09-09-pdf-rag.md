# Shastra RAG v1 Implementation Plan

> **For agentic workers:** Implement inline in this session. Retrieval-only, build-time index. Do not commit unless the user asks.

**Goal:** Chatbot quotes Gita verses and Ramayan PDF pages from a shipped JSON index.

**Architecture:** Node script builds `public/rag/shastra-index.json`. Client keyword-searches it. No new backend.

**Tech Stack:** pdfjs-dist (already in the project), Vite static JSON, existing React chatbot.

## Global Constraints

- Hostinger static hosting only
- Retrieval only in v1 (no cloud LLM)
- Do not ship the 8–14MB PDFs for search
- Skip non-Unicode Gita Press PDF text
- Do not commit unless asked

## Files

- Create: `scripts/shastra-sources.json`
- Create: `scripts/build-shastra-index.mjs`
- Create: `public/rag/shastra-index.json` (generated)
- Create: `src/utils/shastraSearch.ts`
- Modify: `src/components/GitaChatbot.tsx`
- Modify: `src/App.tsx` (mount chatbot app-wide)
- Modify: `src/components/ChapterList.tsx` (remove Gita-only chatbot)
- Modify: `package.json` (`index:shastras` script)
---

### Task 1: Indexer + search + chatbot

- [x] Spec written
- [ ] Indexer extracts Ramayan + Gita JSON
- [ ] Search utility
- [ ] Chatbot UI with source chips and citations
- [ ] Verify with sample queries
